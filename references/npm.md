# NPM | Referência Rápida

``` bash
# Atualiza o node package manager para sua última versão
sudo npm i -g npm

# Mostra a versão de um pacote
npm info gulp version
npm show gulp version
npm view gulp version

# Mostra as informações de um pacote
npm info gulp
npm show gulp
npm view gulp
```
